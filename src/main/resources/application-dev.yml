spring:
  datasource:
    url: jdbc:mysql://localhost:3306/greenpath?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Ckddlfdl1!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  # ── OAuth2 소셜 로그인 설정 ──────────────────────────────────
  security:
    oauth2:
      client:
        registration:
          # 카카오 설정 (https://developers.kakao.com 에서 앱 생성 후 발급)
          kakao:
            client-id: ${KAKAO_CLIENT_ID:0aa8ea802538108605e5f35f87f2b971}
            client-secret: ${KAKAO_CLIENT_SECRET:ZVyzY0pPl83Gaq9xk5g9jvdz4wgGn3Is}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
            client-name: Kakao

          # 네이버 설정 (https://developers.naver.com 에서 앱 생성 후 발급)
          naver:
            client-id: ${NAVER_CLIENT_ID:72UsVjkEvRHW3XabyH9J}
            client-secret: ${NAVER_CLIENT_SECRET:9OlNWO4Nyp}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope:
              - name
              - email
            client-name: Naver

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

# ── JWT 설정 ───────────────────────────────────────────────────
# secret: 256비트(32바이트) 이상의 Base64 인코딩 문자열
# 개발용 키 — 실제 배포 시 환경변수(JWT_SECRET)로 교체 필수!
jwt:
  secret: ${JWT_SECRET:ZGV2LWdyZWVucGF0aC1zZWNyZXQta2V5LW11c3QtYmUtYXQtbGVhc3QtMjU2Yml0cw==}
  access-token-validity-seconds: 1800 # 30분
  refresh-token-validity-seconds: 1209600 # 14일

# ── 프론트엔드 URL ─────────────────────────────────────────────
frontend:
  url: http://localhost:3000

cors:
  allowed-origins: http://localhost:3000

# ── OpenAI 설정 ────────────────────────────────────────────────
openai:
  mock: true
  api-key: ${OPENAI_API_KEY:your_api_key_here}
  embedding-model: text-embedding-3-small

# ── 기상청 API 설정 ─────────────────────────────────────────────
kma:
  service-key: ${KMA_SERVICE_KEY:f79d1cec8499e787306bc0d899fe04ecfe82a43083568d8c1132935d83379b17}
